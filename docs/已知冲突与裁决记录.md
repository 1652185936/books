# 已知冲突与裁决记录

| 项目 | 内容 |
|---|---|
| 文档编号 | MX-ADR-001 |
| 版本 | 1.7.0 |
| 日期 | 2026-06-19 |
| 当前范围 | 规格评审报告 v1：6 阻断 + Batch D1~D6 整改 + D7 全量一致性审计**全部完成**，待复核 |

## 裁决原则

- 严格按评审报告“核验校正”后的事实和严重度处理。
- 只记录已作出的明确裁决；未处理的一般/重大项保持开放。
- 主规格与附录冲突时按 `CLAUDE.md` 的优先级链处理。

## 阻断项处理记录

### ADR-R001：复习卡创建责任

- 评审项：R-001。
- 状态：已修订，待复评。
- 裁决：
  - `package` 卡由 `ImportService` 在 `indexing` 阶段写入。
  - `wrong-item` 卡由 `AttemptOrchestrator` 在错误/部分正确判分事务内写入。
  - 第一版不支持 `user` 手工建卡来源。
  - 创建统一通过 `ReviewRepository.upsertFromSource`，按 `(source_type, source_id)` 幂等。
- 修改文件：
  - `02-概要设计说明书.md` §4.4。
  - `03-详细设计说明书.md` §3.19.1。
  - `04-接口与DTO契约.md` §7。
  - 学习包规范 §6.3 与 `chapter.schema.json` flashcards 结构。

### ADR-R013：导入解压与校验时序

- 评审项：R-013。
- 状态：已修订，待复评。
- 裁决：
  - manifest 检查后解压到隔离暂存区。
  - hash、Schema 和业务校验全部针对暂存区执行。
  - 用户确认后只做索引、原子提升和事务提交，不再次解压。
  - 状态名区分 `extracting_to_staging` 与 `promoting_to_final`。
- 修改文件：`03-详细设计说明书.md` §2.3、§2.4。

### ADR-R025：完成度公式

- 评审项：R-025。
- 状态：已修订，待复评。
- 裁决：
  - 同时包含讲解与练习的章节：阅读 40%，练习提交 60%。
  - 无练习章节：阅读 100%；无讲解章节：练习提交 100%。
  - 书籍完成度只平均 `lesson/review` 章节。
  - `section_progress` 是阅读完成证据的权威来源，exercise attempts 是练习提交证据。
  - 统一由 `ProgressCalculator` 重算，页面不得自行累加。
- 修改文件：
  - `01-需求规格说明书.md` F04。
  - `03-详细设计说明书.md` §3.12.1、§3.12A。
  - `04-接口与DTO契约.md` §4。

### ADR-R050：接口与 DTO 契约

- 评审项：R-050。
- 状态：已修订，待复评。
- 裁决：
  - 新增 `04-接口与DTO契约.md` 作为跨模块类型唯一真值。
  - 定义导入、书籍、内容、进度、手写、识别、练习、逐空判分、复习和备份 DTO。
  - `GradingService` 负责异步编排，`GradingEngine` 保持同步纯函数。
- 修改文件：
  - `04-接口与DTO契约.md`。
  - `02-概要设计说明书.md` §8。
  - `03-详细设计说明书.md` §7。

### ADR-R062：90% 识别准确率验收

- 评审项：R-062。
- 核验校正：采纳报告校正，指标实际出处限定为 SRS 与 LLD，不错误引用 HLD 原指标。
- 状态：已修订，待复评。
- 裁决：
  - 固定数据集为 `MX-HWR-EN-2000-v1`，共 2000 个真实笔迹空位。
  - 主指标为标准化后的空位精确识别准确率，门槛 90%。
  - 同时要求端到端成功率、P95 时延和识别失败不误判。
  - 数据集、设备、模型和结果均需可审计。
- 修改文件：
  - `05-手写识别验收规范.md`。
  - SRS NFR-P-007。
  - HLD §3.3。
  - LLD §4.2。

### ADR-R063：非手写题型范围

- 评审项：R-063。
- 状态：已修订，待复评。
- 裁决（业主复核后更新，覆盖前述「砍题型」方案）：
  - **保留全部九种题型，不砍**；`choice`/`true-false`/`match`/`order-words` 恢复进 v1 Schema 白名单。
  - 五种 `*-handwriting` 在 v1 完整实现作答与判分；四种非手写题型导入后保留、占位展示，**交互作答与判分列入后续迭代**。
  - IMP-020 改为仅拦截「协议九种之外的未知题型」。
  - R-004、R-019 由「随 R-063 关闭」改为「后续迭代项」（保留题型、暂不实现交互，非删除）。
- 修改文件（含回退）：
  - `CLAUDE.md` §2 第 11 条、SRS FR-F06-09、LLD §2.5/§3.8、`04-接口与DTO契约.md` ExerciseType、
    两份学习包规范 §6.1、`chapter.schema.json`、`CHANGELOG.md`。

## Batch D1：数据流（R-002～R-012）

- 状态：已修订，待复评。
- 新增《06-协议到数据库映射规范》作为协议→DB 字段映射唯一真值。
- 裁决与落地：
  - R-002 复习卡落库 / R-004 非手写题型作答：经核已分别由 Codex 在 `chapter.schema` flashcards + 协议 §6.3 + LLD §3.19.1、以及 R-063 题型白名单收窄 + IMP-020 关闭。
  - R-003 partialRules→grading_rules：映射规范 §4.4（priority=数组下标、`feedback_code=partialRule.id`、whenTokensEqual→tokens-equal/missing-token）。
  - R-005 content_blocks 派生：映射规范 §3（plain_text 按 type 派生、source_pages 继承自 section.sourceRef）。
  - R-006 升级冗余列同步：LLD §9 改为按 `external_id` 原地 UPDATE（映射规范 §10）。
  - R-007 错题规则 + 受控词表：LLD §3.21、§3.24（新增 `wrongbook.masteryStreak`）、映射规范 §5/§6。
  - R-008 report.reviewQueue 落库：映射规范 §8 定义规则与 `content_review_flags` 表（表 DDL 归 Batch D4）。
  - R-009 learning_events 权威源：LLD §3.13、映射规范 §9。
  - R-010 sections.confidence 来源：映射规范 §2.1。
  - R-011 accepted 标准化 / R-030 主答案 / R-035 去重：映射规范 §4.2。
  - R-012 bookmarks 章节级收藏：LLD §3.18 增 `scope` + 部分唯一索引。
  - R-029 contentVersion：`manifest.schema` + 协议 §3 新增 `manifest.contentVersion`。
- 修改文件：`06-协议到数据库映射规范.md`（新增）、`03-详细设计说明书.md` §1.3/§3.13/§3.18/§3.21/§3.24/§9、`manifest.schema.json`、学习包规范 §3。
- 待 Batch D4 落地：`exercises.grading_options_json` 列、`content_review_flags` 表 DDL。

## Batch D2：状态机与事务（R-013～R-024）

- 状态：已修订，待复评。
- R-013 解压时序：阻断项已完成。
- R-014 取消边：LLD §2.3 补 copying/validating_*/extracting/awaiting_confirmation→cancelled；§2.6 定义统一清理。
- R-015 失败重试：§2.6 区分可重试（IMP-013/016）与终态拒绝，复用 `jobId`，要求持久化 URI 授权。
- R-016 升级独立状态机：§2.7 `snapshot_old→promoting_to_final→restore_old`，`books.status=updating` 生命周期。
- R-017 后台导入驱动 + 重启恢复：§2.6 独立后台任务、`import_jobs` 持久化、重启扫描非终态、`awaiting_confirmation` 24h 超时。
- R-018 多空题聚合：§4.2/§4.5 逐空 `perBlank` + 整题聚合 + 失败空不误判整题。
- R-019 非手写题型作答：转为后续迭代项（业主裁决 R-063 保留题型、交互后续迭代，见 ADR-R063）。
- R-020 空答案字段：LLD §3.8 增 `exercises.allow_empty`；`chapter.schema` 增 `allowEmpty`。
- R-021 draft 生命周期/恢复：§4.1 进入书写建 draft attempt + 快照，重启扫描恢复。
- R-022 orphaned 批注 / highlights relocation：LLD §3.17 增 `highlights.relocation_state`；§6.8 单列入口。
- R-023 删除/导出/恢复/清缓存：§8.1 两阶段删除 + BR-010 参数、RestoreMode 语义、清缓存白名单。
- R-024 识别超时退出：§4.2 硬超时 5s + cancel + `processing` 重启复位。
- 修改文件：`03-详细设计说明书.md` §2.3/§2.6/§2.7/§3.8/§3.17/§4.1/§4.2/§4.5/§6.8/§8.1、`chapter.schema.json`。

## Batch D3：接口与服务契约（R-050～R-061）

- 状态：已修订，待复评。
- 核验校正口径：33 条下修已逐条遵循；audit-findings.json 的 `confirmed[]` 顺序与 R-### 不一致，已按**标题**而非下标对照证据。
- **已核销（前序批次/Codex 已解决，本批仅核验）**：
  - R-050（DTO 无结构，阻断）：04 契约已定义全部跨模块 DTO，HLD §8 / LLD §7 已声明以 04 为唯一真值（ADR-R050）。
  - R-054（判分同步/异步签名打架）：HLD §8 与 04 §6 已统一 `GradingService`（异步编排）+ `GradingEngine`（同步纯函数）；本批在 LLD §7.2 补 `GradingEngine` 接口与委托说明，闭合。
  - R-058（asset 读取/全文搜索缺接口）：04/LLD §7.1 已有 `ContentRepository.getAssetUri/getCoverUri`、`BooksRepository.searchChapters` 与 `SearchResult` 类型；目录搜索（chapters 级）与内容范围已界定。
  - R-059（recognize 写死 'en'）：04 §5 `recognize(input, mode: HandwritingMode)`，枚举 english/number/mixed 与 `handwriting_mode` 对齐。
  - R-060（cancel 无 requestId 句柄）：04 §5 `RecognitionHandle{ requestId, result, cancel() }`。
- **本批新整改（解决而非降级）**：
  - R-051 笔记/高亮/收藏(F08) 缺接口：04 新增 §8『批注 DTO』（Note/Highlight/Bookmark + 锚点/重定位）与 `AnnotationRepository`/`AnnotationService`；LLD §7 同步镜像。笔迹采集/橡皮擦/撤销归 `HandwritingService.StrokeSession`。
  - R-052 错题本(F10)/统计(F11) 缺接口：04 新增 §9『错题本与统计 DTO』+ `WrongBookRepository`（query/getDetail/listForBatch/markMastered，且 markMastered 不覆盖 attempts 历史）与 `StatisticsService`（recordEvent 唯一写 learning_events / getMetrics / recompute）；LLD §7 镜像。
  - R-053 设置(F12)/复习计数 缺接口：04 新增 §10『设置 DTO』+ `SettingsService`（键级校验 get/getAll/set），`ReviewScheduler.getTodaySummary`（今日到期/完成计数）；会话内卡片去重明确为会话层内存状态，不设独立接口。
  - R-056 导入失败码未覆盖全部 failed 入口：LLD §2.5 新增 IMP-017 复制失败、IMP-018 解压失败、IMP-019 建索引失败，并明确各 failed 入口→码映射；§2.6 重试分类相应更新。
  - R-057 识别失败码无枚举 + 文案来源缺失：LLD §3.14 标注 `recognition_error_code` 取值集（对齐 04 §5 `RecognitionErrorCode`），§4.2 三种失败情形映射到枚举；新增 §3.14A『error_code→i18n 资源键映射表』覆盖全部 IMP-* 与识别码，满足 §12 检查项。
  - R-061 P-F07 路由复用/query 歧义：LLD §6.6/§6.7 增加 `from`（chapter/wrongbook）来源标识决定返回栈，新增 §6.13『路由参数约定』规定 `bookId` 省略=全部、`range∈{day,week,month}` 缺省 week / 非法回退 week；删去评审夸大表述（`range` 缺省、lesson `section` 缺省经核已覆盖）。
- 修改文件：`04-接口与DTO契约.md`（§7 增 `getTodaySummary`/`TodayReviewSummary`，新增 §8/§9/§10，备份与一致性规则顺延为 §11/§12，版本 1.1.0→1.2.0）、
  `03-详细设计说明书.md` §2.5/§2.6/§3.14/§3.14A/§4.2/§6.6/§6.7/§6.13/§7.1/§7.2。
- 滚动到 Batch D4（DB 约束）落地（属顺序排期，非降级）：
  - R-055：`exercise_attempts` 增加逐空结果存储（per-blank `grade`/`score`/`feedback_code`，或新增子表）；GradeResult `perBlank` 已由 04 §6 定义。
  - R-057：`recognition_error_code` 的 `CHECK IN (...)` 形式化约束随其它枚举一并补齐。

## Batch D4：数据库约束（R-038～R-049 + 滚入项）

- 状态：已修订，待复评。
- 核验校正口径：严格按报告「核验校正」处理；R-045/046/048/049 已下修为 minor 并按收窄口径整改，删去夸大表述。audit-findings.json `confirmed[]` 顺序与 R-### 不一致，仍按**标题**对照。
- **本批整改（解决而非降级）**：
  - R-038 软删除语义割裂：LLD §8.1 明确「删除模型」——`books` 为唯一软删除锚点，BR-010 二选一（①保留学习记录=软删 `status='deleted'`+`deleted_at`，下游随属主过滤隐藏、不各自加 `deleted_at`；②彻底删除=物理 `DELETE` + `ON DELETE CASCADE`），消除「软删后统计虚高」「硬删误清历史」两风险；`notes`/`highlights` 的 `deleted_at` 仅作批注级软删/重定位，与书级模型正交。
  - R-039 枚举列 NULL 穿透：§1.3 强化「枚举必须 `NOT NULL`」原则（含 SQLite CHECK 对 NULL 不为 FALSE 的说明）；为 §3.4/3.5/3.7/3.8/3.9/3.11/3.12/3.14/3.15/3.16/3.21/3.23 全部枚举列补 `NOT NULL`，状态列补 `DEFAULT`（not-started/not-requested/draft/unreviewed/stable/active/pending 等）。
  - R-040 accepted 唯一键撞键：§3.10 增去重交叉引用——按《06》§4.2 第 3 条标准化后合并保留首个 `display`，**不算导入失败**（保 `UNIQUE(blank_id, normalized_answer)`，不删变体能力）。
  - R-041 handwriting_records 多态：增 `review_card_id` FK（review-answer 第三态），三态 `CHECK(=1)` 互斥；统一删除语义——软删 note 不清笔迹，笔迹物理生命周期绑定属主物理删除。
  - R-042 Schema const 锁版本：manifest/book/chapter 的 `formatVersion`/`schemaVersion` 由 `const` 改 `pattern ^1\.\d+\.\d+$`（主版本交 IMP-006 白名单判断），顶层显式 `additionalProperties:true`。
  - R-043 校验链断裂：新建 `report.schema.json`、`checksums.schema.json`；`chapter.schema.json` 用 `if/then` 仅对五种手写题型强约束 `prompt.segments`/`answers.<key>.accepted`/`grading.partialRules` 结构，四种非手写题型保持通用对象（**不破 R-063 保留题型**，已用 jsonschema 验证）；LLD §2.4 step8 声明仅用内置 Schema、忽略包内 `schemas/`（封信任边界，BR-002）。
  - R-044 regex ReDoS：HLD §11 + LLD §3.11 增导入期静态校验（源长上限、禁嵌套量词）+ 运行期输入上限/50ms 硬超时可取消/超时降级，不阻塞「确认答案」（NFR-P-004）。
  - R-045 plain_text 索引（minor）：删除误导性 `plain_text` 搜索 B-Tree 索引；§3.6 明确搜索限章节 `title/title_zh/keywords`（`BooksRepository.searchChapters`），正文 FTS 留后续。
  - R-046 命名映射（minor）：§1.3 增「跨层命名映射约定」（DB snake / JSON camel / 枚举 kebab，mapper 自动转换，枚举常量同源生成 CHECK），按校正删去路由一支。
  - R-047 chapter_count 失真（minor）：§3.3 改为每次导入/升级提交事务内 `SELECT COUNT(*)` 重算 + 集成测试断言；§9 增「冗余列同步清单」。
  - R-048 reading_progress 锚点（minor）：§9 增升级时按 `external_id` 重映射 `last_section_id`/`last_block_id`、无法映射回退 `scroll_anchor_json`/章节首；不加 `deleted_at`（属主过滤覆盖）。
  - R-049 assets 冗余唯一键（minor）：删除 `UNIQUE(book_id, sha256, relative_path)`（`UNIQUE(book_id, relative_path)` 超集），删去捏造的 §10 去重交叉引用。
  - 滚入项落地：§3.8 增 `grading_options_json` 列（R-026/《06》§4.1）；新增 §3.25 `content_review_flags` 表（R-008/《06》§8）；§3.14 增 `blank_results_json` 逐空判分存储（R-055，统计正确率口径整题/按空分别标注）；`recognition_error_code` 加 `CHECK`（R-057 形式化完成）。
- 修改文件：`02-概要设计说明书.md` §11、`03-详细设计说明书.md` §1.3/§2.4/§3.3/§3.5/§3.6/§3.7/§3.8/§3.9/§3.10/§3.11/§3.12/§3.14/§3.15/§3.16/§3.21/§3.23/§3.25/§8.1/§9、`packages/studybook-schema/`（manifest/book/chapter 改 + report/checksums 新增）。
- 校验：5 份 Schema 均通过 `Draft202012Validator.check_schema` 与样例校验（手写题型强约束生效、choice 等非手写题型仍可导入、blank 缺 id/缺 accepted 正确报错）。

## Batch D5：页面与交互（R-025~R-037 页面/歧义子集 + 滚入项）

- 状态：已修订，待复评。
- 核验校正口径：严格遵循；R-036/037 等已下修 minor 并按收窄口径处理。HANDOFF §4 规定 R-025~R-037 中 DB 类已并入 D4，本批只做页面/交互/单位/渲染/歧义子集。
- **本批整改**：
  - R-026 章节状态判定：LLD §3.12.2 定义 `reading_progress.status` 四态进入条件（review-due 覆盖态，真值源 `wrong_items.next_due_at`/`review_cards.due_at`），与 `completion_percent` 同源由 `ProgressService` 写入，掌握章节数另走掌握度口径。
  - R-027 连续天数/有效时长：§3.13.1 定义本地时区切日（含 DST）、「有效一天」门槛（≥5min 或 ≥1 题提交）、active 段时长聚合（暂停前 2min 空闲不计）。
  - R-028 判分阈值+内置语法规则：§4.4 列出内置语法规则集（5.1~5.4，feedback_code 取《06》§5）；明确编辑距离 ≤ maxEditDistance 非精确匹配判 `incorrect`+提示、不改 grade/score。
  - R-034 空 sections/默认小节：§6.5 定义 `?section` 缺省进数组下标 0、`reading_progress.last_section_id` 优先恢复；空 sections 章节走 `ChapterEmptyView`、不白屏，与「章节损坏」区分。
  - R-035 单位换算：§1.3 定义 `1em=16vp`/`emToVp` 与「逻辑像素=vp」；§6.6 clamp 改 `clamp(emToVp(widthEm),96vp,360vp)`；§4.1 坐标单位明确为 vp（解决同节「逻辑像素」与「32vp」混用）。
  - R-037 IMP-015 触发源：§2.5 定义 reviewQueue 非空 / 任一 `quality.reviewStatus='needs-review'` / report `status='completed-with-warnings'` 三者任一触发，落 `content_review_flags`；删去「§2.4 第3步崩溃」误定位（report 结构由 D4 report.schema.json 校验）。
  - 滚入项 + R-036(2)：§6.5 渲染映射表补 `bullet-list`→`BulletList`、`audio-text`→`AudioTextBlock`，与协议 §5.2 的 11 种 `block.type`、04 契约 `ContentBlock.type` 逐一对齐。
- 修改文件：`03-详细设计说明书.md` §1.3/§2.5/§3.12.2/§3.13.1/§4.4/§6.5/§6.6/§4.1。
- 可执行基准值（已给定、可随 token 微调）：R-027「有效一天 5 分钟」、R-035「1em=16vp」。
- **核销（前序已解决，不重做）**：R-025（完成度公式，ADR-R025）、R-029（grading_options_json，D4）、R-030/R-031（partialRules→grading_rules 映射+feedback_code 来源，06 §4.4/§5）、R-032（version 对应，D1+06 §2）、R-033（accepted 去重/is_primary/chapter_count 已落地）、R-036(1)（source_pages 继承，06 §3.1）。

## Batch D6：测试 / 验收 / NFR / 文档完整性（R-064~R-073）

- 状态：已修订，待复评。
- 核验校正口径：严格遵循；R-068/069/070 下修 minor、R-073 下修 info，按收窄口径处理。R-###↔标题以报告 `#### [R-0xx]` 小标题为准（与初拟猜测有偏移，已按 title 核对）。
- **本批整改**：
  - R-064 NFR 不可追溯：SRS §8.2~§8.5 为可靠性/安全/无障碍/兼容性补编号（NFR-R/S/A/C），修正 §8.1 NFR-P 乱序；§9 矩阵扩为双向追踪（需求→HLD→LLD→测试用例→验收）并新增 NFR 行。
  - R-065 识别选型/降级：HLD §3.3.1 选型四指标论证 + 「识别能力不可用」降级态（保留笔迹、停用判分、其余可用），HLD §9 加行、SRS 加 NFR-R-005。
  - R-066 i18n 规范：LLD §3.14B 资源目录/键命名/占位符/文案边界 + 资源键→中英文文案表，承接 §3.14A 映射真值、不重复定义。
  - R-067 统计口径：LLD §3.13A 定义 `StatsResult` 各字段公式（正确率默认整题口径、掌握度、薄弱点复用 §3.21 受控词表不新建表），引用 §3.13.1 底层口径；修正 04 `currentStreakDays` 悬挂引用 §3.12.1→§3.13.1/§3.13A。
  - R-068 术语表：SRS §11 收录 5 类易歧义术语（连续天数/掌握度/薄弱知识点/目标设备/测试集），排除已内联定义项。
  - R-069 V001 迁移：LLD §9 澄清命名为示例、V001 一次性按 §3 建全表、V002/V003 仅命名示范。
  - R-070 测试可执行：LLD §11.1/§11.2 改为带「关联规格 + 通过判定」的用例矩阵（TC-U-001~008/TC-I-001~007，含 SRS §6.7 四条判分断言），§11.3 UI 验收改可观察判据。
  - R-071 缓存/版本：LLD §9A 缓存容量 200MB+LRU+失效、笔迹文件 version 向后兼容、备份 formatVersion 兼容判定（对接 04 `BackupValidationResult`）。
  - R-072 容量阈值：LLD §2.1 阈值表加「取值依据」列；SRS 加 NFR-P-008（500MB≤240s，补 100–500MB 空档）。
  - R-073 修订历史：SRS/HLD/LLD 头补「修订历史」；不碰 CHANGELOG（在禁改清单）。
- 修改文件：`01-需求规格说明书.md` §8.1~§8.5/§9/§11/头、`02-概要设计说明书.md` §3.3.1/§9/头、`03-详细设计说明书.md` §2.1/§3.13A/§3.14B/§9/§9A/§11/头、`04-接口与DTO契约.md`（修 1 处悬挂引用）。
- 衔接校验：R-066 引用 §3.14A 不重建映射；R-067 引用 04 `StatsResult`/§3.14 正确率口径、薄弱点复用 §3.21、不改 DTO；R-071 引用 04 `BackupValidationResult` 字段、只补判定规则。
- 可执行基准值（已给定）：缓存 200MB、NFR-P-008 500MB≤240s。
- **无核销项**：R-064~R-073 均有未闭合缺口（R-062/R-063 前序已完成，不在本批）。

## Batch D7：全量一致性审计

- 状态：已完成，待复评。
- **机器可校验项（全部通过）**：
  - 跨文档 §引用（本档 + `04 契约 §N` + `《06》§N` + SRS §9 矩阵的 HLD/LLD 列）全部命中，零悬挂引用。
  - Markdown 相对链接零失效；ADR 对 R-001~R-073 全覆盖；两份《AI-PDF 学习包规范》逐字一致。
  - 各 Schema 通过 Draft2020-12 校验。
- **深度语义审计（子 agent，发现并修复 2 处真实跨文档矛盾）**：
  - **矛盾 1（高，R-063 残留）**：SRS 仍有两处旧口径与 FR-F06-09/CLAUDE.md §2.11 冲突——
    «产品范围» 称「选择/判断/连线/词序拖拽不属于第一版」、F06 验收称「白名单外题型导入业务校验阶段阻断」。
    **已修**：SRS 产品范围改为「五种手写题型 v1 完整实现、四种非手写题型保留占位」；F06 验收改为「仅协议九种**之外**的未知题型触发 IMP-020 阻断，九种以内的四种非手写题型属白名单之内、不阻断」，与 LLD §2.5/§3.8 对齐。
  - **矛盾 2（中，序列化字段名）**：04 契约 `StrokePoint.pressure`（运行期 DTO）与 LLD §4.1 `.stroke.json.gz` 的 `p`（落盘格式）字段名分歧、且文件每笔多 `tool`/`color`/`width`，无文档声明映射。
    **已修**：LLD §4.1 增「DTO ↔ 落盘格式字段映射」说明（`pressure→p`，per-stroke `tool/color/width` 为落盘格式专有，由 `handwriting_records` 承载），把隐式约定升为成文。
  - 其余跨文档项（九种题型集合、枚举三方字面、数值/默认值、04↔HLD↔LLD 接口签名、软删/搜索/降级/统计口径/IMP-015 触发五项决策、Schema if/then 与占位策略、TC-*/资源键引用）**全部语义一致**。
- **版本治理**：SRS/HLD/LLD 由 1.1.0 升 1.2.0（对齐 04），状态由「阻断项修订完成」更新为「评审 v1 整改完成（6 阻断 + D1~D6）」；三份规格「修订历史」统一为 `## 修订历史` 表格形式（1.0.0/1.1.0/1.2.0）。05=1.1.0、06=1.0.0（评审中创建、本轮无结构性改动）保持。
- 修改文件：`01-需求规格说明书.md`（产品范围、F06 验收、头/修订历史）、`02-概要设计说明书.md`（头/修订历史）、`03-详细设计说明书.md`（§4.1 字段映射、头/修订历史）。

## 实现期冲突：i18n 资源键命名（P0-5）

- 发现阶段：实现 P0-5「i18n 资源骨架」时。
- 冲突双方：
  - **LLD §3.14B**（R-066）规定 i18n 资源键用**点分层级**（`域.子域.含义`，如 `error.import.invalidZip`），
    并以 HarmonyOS `entry/src/main/resources/{base,zh_CN,en_US}/element/string.json` 为载体；§3.14A 同源固化该点分映射。
  - **HarmonyOS 平台约束**：`restool` 官方文档明确「资源名必须匹配 `[a-zA-Z0-9_]`，仅允许字母/数字/下划线」，
    且引用语法 `$r('app.string.NAME')` 以点号分隔层级——点分键既非法、又与引用语法歧义。
    （核实来源：HarmonyOS Guides / restool 资源错误码「Invalid resource name」。）
- 优先级链裁决（`CLAUDE.md` §3）：`CLAUDE.md §2.1`「技术栈 ArkTS + ArkUI」为最高优先级强制项，
  平台对资源名的硬约束高于 LLD §3.14B 对**键字面语法**的选择。故：
  - **保留点分键为 i18n 逻辑键真值**（§3.14A/§3.14B 不改语义、不改文档）；`AppError.messageKey` 始终携带逻辑键。
  - **物理资源名用确定性转写 `.`→`_`**（`error.import.invalidZip` → `error_import_invalidZip`），落盘进三份 `string.json`；
    转写一一对应、可逆、语义零变化。
  - 代码侧桥接：`AppErrors.physicalKeyFor(logicalKey)` 单点完成 `.`→`_`，页面 `$r('app.string.' + physicalKeyFor(messageKey))`。
- 影响面：仅新增「逻辑键→物理名」编码层，未删字段/功能/题型，未改 §3.14A 映射真值与 §3.14B 规范语义（解决而非降级）。
- 落地文件：`app/entry/src/main/resources/{base,zh_CN,en_US}/element/string.json`（25 条 §3.14A 键 + `action_confirm`/`state_loading`/`state_empty`/`state_error` 通用键）、
  `app/entry/src/main/ets/utils/AppErrors.ets`（新增 `physicalKeyFor` 桥接 + 模块注释）。
- 备注：§3.14B 文案表为「节选」，本轮按其口径**落盘补齐** §3.14A 全部 IMP-*/识别码中英文文案；
  其中 6 条规范已给定文案（invalidZip/hashMismatch/recognition.timeout/recognition.unavailable/action.confirm/state.empty）逐字一致，其余为补齐文案。

## CHANGELOG 说明（未纳入本轮提交）

`CHANGELOG.md` 当前混有「英文手写字帖（07/F13）」工作流的未提交改动（已占用 `1.2.0-draft` 条目），与评审整改无关且在禁改清单内。为不污染该工作流，本轮**未改 CHANGELOG.md**；评审整改的完整变更日志由本 ADR（逐条 disposition）与三份规格的「修订历史」承载。建议字帖工作流合并时一并补评审整改的 CHANGELOG 条目。

## 整改总结

规格评审报告 v1 共 73 条发现（R-001~R-073），经 6 条阻断 + Batch D1~D6 整改 + D7 全量一致性审计，**全部处理完毕、待业主/评审复核**。
全程遵循「解决而非降级」：无字段/功能/题型被删（R-049 删冗余唯一键为评审预期、不损能力），九种练习题型完整保留（R-063）。
唯一例外性裁决 R-063 已经业主复核确认。复核通过前不视为评审正式关闭。

## 实现期张力：升级删章节与 chapter_count 口径（P1-5）

- 发现阶段：实现 P1-5「升级 snapshot/promote/restore 子流程」的内容原地落库（`BookContentRepository.upgradeBook`）时。
- 张力双方（均为已定稿规格，非起草矛盾）：
  - **《06》§10**（R-006）：升级按 `external_id` 原地 UPDATE，旧包存在、新包缺失的「被删章节/练习」对应 `exercises` 置 `enabled=0` 并**保留其 attempts/wrong_items，不物理删除**（满足 FR-F03-06、验收 5）。
    `exercise_attempts.chapter_id FK chapters ON DELETE CASCADE` ⇒ 要保留被删章节下的 attempts，就**必须保留该章节行**（删行会级联清空答题历史）。
  - **LLD §3.3**：`chapter_count` 是 `chapters` 行数的反规范化冗余，「在提交事务内由 `SELECT COUNT(*) FROM chapters WHERE book_id=?` 重算写回」，且 FR-F04-01 直接展示该值。
  - 二者并存 ⇒ 升级删章节后，被保留的「幽灵章节」会被计入 `chapter_count`，使 FR-F04-01 展示值偏大。
- 优先级链裁决（`CLAUDE.md` §3，`01-SRS > 03-LLD > 06`）：用户**学习历史不丢失**（FR-F03-06、验收 5，需求层）优先于 `chapter_count` 的精确展示（派生展示值，可后置修正）。故 P1-5：
  - **保留被删章节行**（其 `order_no` 由升级期 `vacate` 移出有效区，规避 UNIQUE(order_no) 重排冲突）、被删练习 `enabled=0`、attempts/wrong_items 不动——以满足 §10/FR；
  - `chapter_count` 暂按 §3.3 字面「COUNT(*) chapters」重算（含被删保留章节），**不在 P1-5 私改 §3.3 语义**；
  - **开放项（待业主/F04 裁决）**：FR-F04-01 的章节计数与目录展示是否应「仅计有效章节」。可选方案：①给 `chapters` 增 `active`/软删标记列（新增迁移），目录与计数按其过滤；②`chapter_count` 改为「活跃章节数」并同步 §3.3 文案。在裁决前，F04 落地可先用「章节 `order_no < UPGRADE_ORDER_OFFSET`」或后续 `active` 列过滤目录展示，避免幽灵章节进入 TOC。
- 影响面：未删字段/功能/题型；未私改 §3.3/§10 语义；仅在 P1-5 升级落库内按优先级链取「保留历史」一侧，并把展示口径开放给 F04/业主（解决而非降级）。
- 落地文件：`app/entry/src/main/ets/repositories/BookContentRepository.ets`（`upgradeBook`/`vacateOrder`/`disableExercises`）、`app/entry/src/main/ets/services/import/UpgradePlan.ets`（`removedChapterIds`/`removedExerciseIds` 推导）。

