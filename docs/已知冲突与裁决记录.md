# 已知冲突与裁决记录

| 项目 | 内容 |
|---|---|
| 文档编号 | MX-ADR-001 |
| 版本 | 1.0.0 |
| 日期 | 2026-06-19 |
| 当前范围 | 规格评审报告 v1 的 6 条阻断项 |

## 裁决原则

- 严格按评审报告“核验校正”后的事实和严重度处理。
- 只记录已作出的明确裁决；未处理的一般/重大项保持开放。
- 主规格与附录冲突时按 `CLAUDE.md` 的优先级链处理。

## 阻断项处理记录

### ADR-R001：复习卡创建责任

- 评审项：R-001。
- 状态：已修订，待复评。
- 裁决：
  - `package` 卡由 `ImportService` 在 `indexing` 阶段写入。
  - `wrong-item` 卡由 `AttemptOrchestrator` 在错误/部分正确判分事务内写入。
  - 第一版不支持 `user` 手工建卡来源。
  - 创建统一通过 `ReviewRepository.upsertFromSource`，按 `(source_type, source_id)` 幂等。
- 修改文件：
  - `02-概要设计说明书.md` §4.4。
  - `03-详细设计说明书.md` §3.19.1。
  - `04-接口与DTO契约.md` §7。
  - 学习包规范 §6.3 与 `chapter.schema.json` flashcards 结构。

### ADR-R013：导入解压与校验时序

- 评审项：R-013。
- 状态：已修订，待复评。
- 裁决：
  - manifest 检查后解压到隔离暂存区。
  - hash、Schema 和业务校验全部针对暂存区执行。
  - 用户确认后只做索引、原子提升和事务提交，不再次解压。
  - 状态名区分 `extracting_to_staging` 与 `promoting_to_final`。
- 修改文件：`03-详细设计说明书.md` §2.3、§2.4。

### ADR-R025：完成度公式

- 评审项：R-025。
- 状态：已修订，待复评。
- 裁决：
  - 同时包含讲解与练习的章节：阅读 40%，练习提交 60%。
  - 无练习章节：阅读 100%；无讲解章节：练习提交 100%。
  - 书籍完成度只平均 `lesson/review` 章节。
  - `section_progress` 是阅读完成证据的权威来源，exercise attempts 是练习提交证据。
  - 统一由 `ProgressCalculator` 重算，页面不得自行累加。
- 修改文件：
  - `01-需求规格说明书.md` F04。
  - `03-详细设计说明书.md` §3.12.1、§3.12A。
  - `04-接口与DTO契约.md` §4。

### ADR-R050：接口与 DTO 契约

- 评审项：R-050。
- 状态：已修订，待复评。
- 裁决：
  - 新增 `04-接口与DTO契约.md` 作为跨模块类型唯一真值。
  - 定义导入、书籍、内容、进度、手写、识别、练习、逐空判分、复习和备份 DTO。
  - `GradingService` 负责异步编排，`GradingEngine` 保持同步纯函数。
- 修改文件：
  - `04-接口与DTO契约.md`。
  - `02-概要设计说明书.md` §8。
  - `03-详细设计说明书.md` §7。

### ADR-R062：90% 识别准确率验收

- 评审项：R-062。
- 核验校正：采纳报告校正，指标实际出处限定为 SRS 与 LLD，不错误引用 HLD 原指标。
- 状态：已修订，待复评。
- 裁决：
  - 固定数据集为 `MX-HWR-EN-2000-v1`，共 2000 个真实笔迹空位。
  - 主指标为标准化后的空位精确识别准确率，门槛 90%。
  - 同时要求端到端成功率、P95 时延和识别失败不误判。
  - 数据集、设备、模型和结果均需可审计。
- 修改文件：
  - `05-手写识别验收规范.md`。
  - SRS NFR-P-007。
  - HLD §3.3。
  - LLD §4.2。

### ADR-R063：非手写题型范围

- 评审项：R-063。
- 状态：已修订，待复评。
- 裁决（业主复核后更新，覆盖前述「砍题型」方案）：
  - **保留全部九种题型，不砍**；`choice`/`true-false`/`match`/`order-words` 恢复进 v1 Schema 白名单。
  - 五种 `*-handwriting` 在 v1 完整实现作答与判分；四种非手写题型导入后保留、占位展示，**交互作答与判分列入后续迭代**。
  - IMP-020 改为仅拦截「协议九种之外的未知题型」。
  - R-004、R-019 由「随 R-063 关闭」改为「后续迭代项」（保留题型、暂不实现交互，非删除）。
- 修改文件（含回退）：
  - `CLAUDE.md` §2 第 11 条、SRS FR-F06-09、LLD §2.5/§3.8、`04-接口与DTO契约.md` ExerciseType、
    两份学习包规范 §6.1、`chapter.schema.json`、`CHANGELOG.md`。

## Batch D1：数据流（R-002～R-012）

- 状态：已修订，待复评。
- 新增《06-协议到数据库映射规范》作为协议→DB 字段映射唯一真值。
- 裁决与落地：
  - R-002 复习卡落库 / R-004 非手写题型作答：经核已分别由 Codex 在 `chapter.schema` flashcards + 协议 §6.3 + LLD §3.19.1、以及 R-063 题型白名单收窄 + IMP-020 关闭。
  - R-003 partialRules→grading_rules：映射规范 §4.4（priority=数组下标、`feedback_code=partialRule.id`、whenTokensEqual→tokens-equal/missing-token）。
  - R-005 content_blocks 派生：映射规范 §3（plain_text 按 type 派生、source_pages 继承自 section.sourceRef）。
  - R-006 升级冗余列同步：LLD §9 改为按 `external_id` 原地 UPDATE（映射规范 §10）。
  - R-007 错题规则 + 受控词表：LLD §3.21、§3.24（新增 `wrongbook.masteryStreak`）、映射规范 §5/§6。
  - R-008 report.reviewQueue 落库：映射规范 §8 定义规则与 `content_review_flags` 表（表 DDL 归 Batch D4）。
  - R-009 learning_events 权威源：LLD §3.13、映射规范 §9。
  - R-010 sections.confidence 来源：映射规范 §2.1。
  - R-011 accepted 标准化 / R-030 主答案 / R-035 去重：映射规范 §4.2。
  - R-012 bookmarks 章节级收藏：LLD §3.18 增 `scope` + 部分唯一索引。
  - R-029 contentVersion：`manifest.schema` + 协议 §3 新增 `manifest.contentVersion`。
- 修改文件：`06-协议到数据库映射规范.md`（新增）、`03-详细设计说明书.md` §1.3/§3.13/§3.18/§3.21/§3.24/§9、`manifest.schema.json`、学习包规范 §3。
- 待 Batch D4 落地：`exercises.grading_options_json` 列、`content_review_flags` 表 DDL。

## Batch D2：状态机与事务（R-013～R-024）

- 状态：已修订，待复评。
- R-013 解压时序：阻断项已完成。
- R-014 取消边：LLD §2.3 补 copying/validating_*/extracting/awaiting_confirmation→cancelled；§2.6 定义统一清理。
- R-015 失败重试：§2.6 区分可重试（IMP-013/016）与终态拒绝，复用 `jobId`，要求持久化 URI 授权。
- R-016 升级独立状态机：§2.7 `snapshot_old→promoting_to_final→restore_old`，`books.status=updating` 生命周期。
- R-017 后台导入驱动 + 重启恢复：§2.6 独立后台任务、`import_jobs` 持久化、重启扫描非终态、`awaiting_confirmation` 24h 超时。
- R-018 多空题聚合：§4.2/§4.5 逐空 `perBlank` + 整题聚合 + 失败空不误判整题。
- R-019 非手写题型作答：转为后续迭代项（业主裁决 R-063 保留题型、交互后续迭代，见 ADR-R063）。
- R-020 空答案字段：LLD §3.8 增 `exercises.allow_empty`；`chapter.schema` 增 `allowEmpty`。
- R-021 draft 生命周期/恢复：§4.1 进入书写建 draft attempt + 快照，重启扫描恢复。
- R-022 orphaned 批注 / highlights relocation：LLD §3.17 增 `highlights.relocation_state`；§6.8 单列入口。
- R-023 删除/导出/恢复/清缓存：§8.1 两阶段删除 + BR-010 参数、RestoreMode 语义、清缓存白名单。
- R-024 识别超时退出：§4.2 硬超时 5s + cancel + `processing` 重启复位。
- 修改文件：`03-详细设计说明书.md` §2.3/§2.6/§2.7/§3.8/§3.17/§4.1/§4.2/§4.5/§6.8/§8.1、`chapter.schema.json`。

## 尚未处理

R-025~R-037 中的剩余项（D3 接口与歧义）、R-038~R-049（D4 数据库约束）、
R-050~R-061（D5 页面与交互）、R-062~R-073（D6 测试/验收/NFR）尚未处理。继续按批次顺序处理，
最后做全量一致性审计；不能因为已关闭批次而视为整份评审通过。

