# ai.studybook 学习包协议

本目录保存 `.studybook.zip` 的 JSON Schema、版本说明和兼容性测试。

协议基本结构：

```text
manifest.json
book.json
report.json
checksums.json
chapters/*.json
assets/**/*
schemas/*.json
```

应用只接受协议版本白名单内并通过完整校验的学习包。

