# 墨学书架 — 实现 Backlog（Ralph 自主迭代）

最终目标：按 `docs/specifications/` 规格完整实现第一版 HarmonyOS 平板应用（ArkTS + ArkUI）。
顺序遵循 CLAUDE.md §4 开发流程：先数据层与可测业务逻辑，再 ArkUI 页面，再测试。
每条尽量是「一轮可完成 + 可验证」的粒度。依赖靠顺序表达（靠前的先做）。

## 阶段 0 — 工程与基础设施
- [x] P0-1 HarmonyOS 工程脚手架：`app/` 下建 DevEco/hvigor 工程（module.json5、app.json5、ArkTS 严格类型配置、目录骨架 ets/{pages,viewmodel,service,repository,db,model,resource,utils}），可被 hvigor 识别。
  > 备注：目录骨架按 LLD §1.2 真值建（pages/components/viewmodels/domain/repositories/services/database/storage/recognition/utils；资源置 `entry/src/main/resources/`，非 ets 内），覆盖 BACKLOG 的非正式命名。未编译验证：缺 HarmonyOS SDK / hvigor，仅做引用自洽与 JSON 校验。
- [x] P0-2 通用类型与 DTO：按《04 契约》§1 定义 `Id/EpochMs/JsonObject/PageRequest/PageResult/AppError`，并为各域 DTO 建 ArkTS model 文件（导入/书籍内容/进度/手写识别/练习判分/复习/批注/错题统计/设置/备份），与 04 字面一致。
  > 落地：`entry/src/main/ets/domain/entities/` 下 11 个文件（common/import/content/progress/handwriting/exercise/review/annotation/wrongbook-stats/settings/backup），逐字段对齐 04 §1~§11（含全部 19 个 Repository/Service/Engine 接口契约）。
  > ArkTS 适配（字段集/语义不变）：交叉类型 `&`→`interface extends`（BookDetail/WrongItemDetail）；索引访问 `ReviewCard['state']`/`ExerciseDetail['gradingOptions']`→具名类型 `ReviewCardState`/`GradingOptions`；联合内联支与内联返回对象拆具名接口（ImportPhaseEvent/RecognitionSuccess/RecognitionFailure/RelocationList/WeakPoint/DailyStat/StrokeCanvas/ContinueTarget）。
  > 验证：11 文件经独立 `tsc --strict --noEmit` 交叉核验通过（跨文件 import/导出、联合、extends、泛型自洽）。未编译验证（缺 HarmonyOS SDK）两处：①`JsonObject` 递归 Record 取值（保持 04 字面，tsc 判 TS2456 为 ArkTS↔tsc 已知分歧，详见文件头注释）；②`SettingsService.get/set` 的泛型索引访问返回 `SettingsValueMap[K]`（保持 04 字面，待 F12 用 SDK 实测，必要时按优先级链登记裁决）。
- [x] P0-3 数据库迁移 V001：按 LLD §3 全部表与索引（books…content_review_flags、§3.25）写 `V001__initial.sql`，含所有 NOT NULL/CHECK/UNIQUE/FK；relationalStore 封装 + 迁移执行器（迁移表、单调递增、只执行一次）。
  > 落地：`resources/rawfile/migrations/V001__initial.sql`（22 业务表 + 17 显式索引，逐表对齐 §3.3~§3.25）+ `ets/database/{SqlScript,MigrationRunner,Database}.ets` + `ets/database/migrations/MigrationRegistry.ets`。元数据表 `db_meta`(§3.2)/`schema_migrations`(迁移表) 由执行器 `CREATE TABLE IF NOT EXISTS` 引导。
  > 约束亮点：枚举全 NOT NULL CHECK（R-039）；accepted_answers 主答案部分唯一索引；bookmarks 章节/块级部分唯一（R-012）；handwriting_records 三态互斥 XOR + 对应 CHECK（R-041）；exercises 九种题型 CHECK（保留非手写四种，R-063）；recognition_error_code 形式化 CHECK（R-057）。
  > 自验证：V001 全量 DDL 经 SQLite（relationalStore 底座）解析建表通过（24 表）+ 约束冒烟测试（FK/枚举/长度/题型/主答案/笔迹 XOR/收藏唯一/版本唯一）全通过。未编译验证：缺 HarmonyOS SDK，ArkTS 侧仅引用自洽核查（relationalStore/resourceManager/TextDecoder 以 API 12 为准）。
- [x] P0-4 Repository 基类与事务封装：统一 Repository 接口约定（页面不碰 SQL）、事务/可回滚封装、`AppError` 领域错误。
  > 落地三件：①`utils/AppErrors.ets` 领域错误工厂——固化 §3.14A error_code→资源键稳定映射（25 条逐行核验一致）+ `DB_*` 通用码兜底 `state.error`；`create/from/isAppError/sanitizeDetail`，detail 脱敏 ≤1000（04 §1），不抛裸字符串。②`database/Database.ets` 增 `withTransaction<T>`/`TransactionWork<T>`——单事务执行、失败整体 `rollBack` 并归一为 `DB_TRANSACTION_FAILED`（LLD §8，relationalStore 经典事务不可重入约定写入注释）。③`repositories/BaseRepository.ets` 抽象基类——`store` 仅 protected 可见（页面不碰 SQL，CLAUDE.md §5）、`queryList/querySingle`（ResultSet finally 关闭、异常归一 `DB_QUERY_FAILED`）、`runInTransaction`、`wrapError`。
  > 自验证：§3.14A 映射经 Python 与规格逐行比对 PASS（25/25 一致、无缺漏无多出）；引用自洽核查通过（TransactionWork/AppErrors/AppError 及 relationalStore API 沿用 P0-3 基线）。未编译验证：缺 HarmonyOS SDK，relationalStore querySql/ResultSet/事务 API 以 API 12 为准。
- [x] P0-5 i18n 资源骨架：`resources/{base,zh_CN,en_US}/element/string.json`；按 LLD §3.14A 落 error_code→资源键映射的全部键中英文文案（§3.14B 文案表）。
  > 落地：三份 `app/entry/src/main/resources/{base,zh_CN,en_US}/element/string.json`——25 条 §3.14A 键（20 IMP-* + 5 识别码）+ §3.14B 通用键 `action_confirm`/`state_loading`/`state_empty`/`state_error`；base=中文基线（含原脚手架键）、zh_CN=中文、en_US=英文。§3.14B 给定 6 条文案（invalidZip/hashMismatch/recognition.timeout/recognition.unavailable/action.confirm/state.empty）逐字一致，其余按口径落盘补齐。
  > 实现期冲突裁决（已登记 ADR「实现期冲突：i18n 资源键命名」）：§3.14B 点分键与 HarmonyOS `restool` 资源名规则 `[a-zA-Z0-9_]`/`$r('app.string.NAME')` 引用语法冲突。按优先级链（CLAUDE.md §2.1 ArkTS/ArkUI 最高）裁决：点分键保留为 i18n 逻辑键真值（§3.14A 不改），物理资源名用确定性 `.`→`_` 转写；`AppErrors.physicalKeyFor` 单点桥接（`error.import.invalidZip`→`error_import_invalidZip`）。解决而非降级、无字段/语义删改。
  > 自验证：三份 JSON 经 Python `json.load` 解析通过；§3.14A 全 25 键在三文件齐备、无 restool 非法资源名、规范给定 6 条文案逐字一致（脚本核验 PASS）。未编译验证：缺 HarmonyOS SDK，`restool` 命名规则以官方文档为准（已 context7 核实）、`$r` 引用桥接待 SDK 编译实测。
- [x] P0-6 路由骨架：按 LLD §6 各页面路由（含 §6.13 `from`/`bookId`/`range` 约定）建 Navigation 路由表与占位页。
  > 落地：①`utils/Routes.ets`——路由逻辑真值：`RouteName`（10 个路由名，值=LLD §6 token，与 route_map `name` 对齐）+ 8 个 `*Params` 接口（ImportValidate/Catalog/Lesson/Exercise/Review/WrongBook/Statistics/Settings，逐字段对齐 §6 路由参数）+ §6.13 归一化纯函数 `normalizeExerciseFrom`（缺省/非法→chapter）/`normalizeStatisticsRange`（缺省/非法→week，不取空数据）/`normalizeOptionalBookId`（空/空白→undefined=全部聚合）+ `ROUTE_TABLE`（路由名→标题资源键，占位页标题单一来源）。②`resources/base/profile/route_map.json`——HarmonyOS 系统路由表，注册 P-F02..P-F12 共 9 条 NavDestination（name/pageSourceFile/buildFunction/data）；module.json5 加 `"routerMap": "$profile:route_map"`。③`pages/Index.ets`——改为 `Navigation(pageStack)` 容器根页，根内容=书架页占位。④9 个占位页（import/ImportSelect·ImportValidate、catalog/Catalog、lesson/Lesson·Exercise、review/Review、wrongbook/WrongBook、statistics/Statistics、settings/Settings）+ `pages/bookshelf/BookshelfPage.ets`（根内容占位）+ 复用 `components/common/RoutePlaceholder.ets`，每页 `@Builder` 导出 + `NavDestination().title($r(...))`。⑤三语 `string.json` 补 10 条 `route_title_*` + `route_placeholder_hint`（中/中/英）。
  > 设计依据：route_map 字段名与系统路由表 page 侧 `@Builder export function`/`NavDestination.onReady(ctx.pathInfo.param)` 取参式经 context7 官方文档核实（arkts-navigation-jump / ui-design-navigation）。P-F06/P-F07 复用 `exercise` 单路由（判分为子状态，沿用同一 `from`，§6.13）；P-F08 批注为 lesson 内模式，无独立路由——均与 §6 一致。占位页本轮不消费 param（F-页面落地时经 onReady 取 `*Params`），路由名/参数类型/§6.13 兜底语义已就绪。
  > 自验证：4 份 JSON 经 Python 解析通过；交叉一致性脚本 PASS（9 条 route_map name⊆RouteName 值、pageSourceFile 均存在、buildFunction 在对应文件定义、NavDestination 存在、ROUTE_TABLE titleKey 与各页 `$r('app.string.*')` 键均在三语 string.json 齐备）；`Routes.ets` 经 `tsc --strict --noEmit` 通过 + 归一化函数 13 例运行时单测全 PASS（含 from/range 非法回退、bookId 空白聚合、默认值）。未编译验证：缺 HarmonyOS SDK，ArkUI 侧（Navigation/NavDestination/@Builder/route_map 解析/`$r` 桥接）仅引用自洽核查，以 API 12 系统路由表为准，待 SDK 编译实测。

## 阶段 1 — 导入内核（F02/F03 的核心，多功能依赖）
- [x] P1-1 ZIP 安全校验：路径穿越/条目数/大小/压缩比/扩展名（LLD §2.1、IMP-001~004），不解压先检查。
  > 落地三件（`services/import/`）：①`ImportLimits.ets`——§2.1「ZIP 资源限制」表 10 项阈值 + 允许扩展名 `.json/.webp/.png/.jpg/.jpeg` 固化常量 + `defaultValidationOptions()`（满足《04》§2 `ValidationOptions` DTO，主版本 [1] 取自 manifest.schema `formatVersion ^1\.x\.y`）。②`ZipCentralDirectory.ets`——纯函数 ZIP **中央目录**解析器（EOCD 末尾扫描 + 文件头枚举，**不解压**即得每条目相对路径/压缩前后大小；以中央目录为准而非可被伪造的本地头/数据描述符；自带最小 UTF-8 解码，零 `@ohos.*` 依赖；ZIP64 哨兵直接判 `exceeds-zip64`）。③`ZipSafetyValidator.ets`——逐条目安全判定，映射 IMP-001~004（FR-F03-01/BR-002/NFR-S-002，§2.4 step2）。
  > 错误码分类裁决（非规格冲突，仅在既有 IMP-001~004 内确定性分派，不新增/不改写语义）：IMP 表 ZIP 阶段仅 4 码；§2.1 把条目数/各类大小/路径深度/文件名·路径长度/允许扩展名统一归「资源限制」，故除路径穿越(IMP-002)、压缩比(IMP-004) 外的资源约束（含伪造扩展名）统一落 IMP-003，子原因记入脱敏 `detail`。
  > 自验证：用 Python 制 11 个真实 ZIP 夹具（正常包/`..`穿越/绝对路径/反斜杠/`.svg`/嵌套zip/压缩炸弹/超大JSON/超深路径/超长段/非ZIP）经算法等价 Node 端口运行 **13/13 PASS**（含正常包未解压枚举 5 条目 + opt 改阈值验 too-many-entries/archive-too-large 分支），对齐 LLD §11 TC-U-001/002。未编译验证：缺 HarmonyOS SDK/tsc，ArkTS 侧仅引用自洽核查（`Uint8Array`/`RegExp`/字面量判别联合以 API 12 为准），实际文件字节读取（URI→bytes）属 P1-3 状态机接线，本轮为不解压安全内核纯逻辑。
- [x] P1-2 内置 JSON Schema 校验器：用 `packages/studybook-schema/` 的 manifest/book/chapter/report/checksums 校验暂存文件；忽略包内 schemas/（LLD §2.4 step8）。
  > 落地三件（`services/import/`）+ 内置 Schema 副本：①`JsonSchemaValidator.ets`——通用 JSON Schema(draft 2020-12 **子集**)纯函数引擎，「Schema+实例→违规列表」，受支持关键字恰好覆盖五个内置 Schema 实际用到的构造（type 含 integer/const/enum/pattern/properties/required/additionalProperties(布尔或子Schema)/minProperties/items/minItems/minLength/minimum/maximum/allOf/if-then-else/布尔 Schema），注解类关键字($schema/$id/title/description/comment/$comment/…)忽略；违规项**只记路径+关键字+结构化原因、绝不含实例值**（CLAUDE.md §5）。②`StudybookSchemaCatalog.ets`——`classifyStagingFile` 文件→种类分类器（manifest.json/book.json/report.json/checksums.json/`chapters/*.json`→对应种类；**`assets/`、包内 `schemas/`、未知文件→undefined 不校验**，封死信任边界 BR-002）+ `SchemaProvider` 来源契约 + `InMemorySchemaProvider` + `BUILTIN_SCHEMA_RAWFILE` 路径常量。③`PackageSchemaValidator.ets`——编排：`parseJson`(失败→IMP-007，不带解析器原文防内容泄露)+`validateAgainstSchema`(违规→IMP-008，脱敏结构化摘要)+`parseAndValidate` 一步式；二者均阻断/终态拒绝。④把五个 Schema 复制进 `resources/rawfile/schemas/` 作**应用内置可信副本**（独立于不可信包内 `schemas/`）。
  > 设计裁决（非规格冲突）：`format`(如 createdAt 的 date-time) 按 JSON Schema 2020-12 默认语义作**注解、不断言**（这些 Schema 未声明 format-assertion 词表），避免误报 IMP-008，时间戳业务有效性归 §2.4 step9 业务校验——「解决而非降级」，无字段/语义删改。错误码在既有 IMP 表内分派（解析失败 IMP-007、结构失败 IMP-008），不新增码。非手写四种题型(choice/true-false/match/order-words) prompt/answers/grading 保持通用对象不强约束（占位通过，R-063）。
  > 自验证：等价 Node 端口镜像三文件逻辑，用五个真实内置 Schema 副本 + 手工夹具回归 **40/40 PASS**（5 个有效包通过 + 20 个逐关键字失败用例命中正确 IMP-008 关键字 + 非手写占位不强约束 + 分类器忽略 `schemas/`/`assets/` + IMP-007 解析失败 + 内置 Schema 缺失兜底 + **关键字覆盖元测试**确认五个 Schema 用到的关键字全部受引擎支持、无静默漏校验）；5 份 rawfile Schema 副本经 Python `json.load` 解析通过。未编译验证：缺 HarmonyOS SDK——`JsonValue` 递归 Record 联合为 ArkTS 动态对象既定写法（标准 tsc 判 TS2456，同 `common.ets` JsonObject 已知分歧）、typeof 联合收窄与 rawfile Schema 读取(resourceManager，留待 P1-3 接线)以 SDK 实测为准。
- [x] P1-3 导入状态机与暂存区：LLD §2.3/§2.4 状态机、隔离暂存、SHA-256 比对（§2.4 step7）、IMP-* 错误码与 §3.14A 文案。
  > 落地四件：①`services/import/ImportStateMachine.ets`——按 §2.3 mermaid **逐边**固化 15 态合法转移表 `IMPORT_TRANSITIONS`（含 `failed→copying` 重试回边 R-015、`selected/awaiting_confirmation` 无 `→failed`）+ `canTransition/assertTransition`（持久化前不变量守卫）/`isTerminal`/`isCancellable`（§2.6：indexing 及之后不可取消）；阶段失败→IMP-* 码（R-056：copying→017/extracting→018/indexing→019/committing→016、磁盘满任意阶段→013）；可重试分类（IMP-013/016/017/019）；持久化状态(snake_case)→04 事件阶段(kebab-case) 映射；`buildFailure/buildStageFailure` 统一经 `AppErrors.create` 走 §3.14A 文案+按 R-015 设 `retryable`。②`ImportStaging.ets`——纯路径派生 `ImportPaths`（暂存 `temp/import/<jobId>`、正式 `files/books/<bookId>/package` 等，§2.2）+ jobId/bookId 单段安全校验 + 相对路径穿越防御 + FS 端口 `ImportFs` + `StagingArea`（prepare/cleanup/listFiles/readText/readBytes，§2.4 step6/§2.6）。③`ChecksumVerifier.ets`——纯比对 `verify`（algorithm=sha256、缺失/不符→阻断、未列项仅诊断、排除 `checksums.json` 自身）+ `toError`→IMP-009（脱敏：仅计数+路径，**绝不含哈希值**）+ `HashComputer` 端口 + `computeStagingHashes`。④`repositories/ImportJobRepository.ets`——状态机持久化边界（§3.22/§3.23）：`transition` 事务内「读当前态→assertTransition 守卫→写」、`insertJob/getJob/listByStates/upsertFile`，页面不碰 SQL。
  > 设计裁决（非规格冲突，均在既有 IMP 表内确定性分派）：SHA-256 阶段「暂存多出未列文件」仅作 `unlisted` 诊断、不在 step7 阻断（checksums `additionalProperties:true` 不闭合枚举，完整性归后续引用校验，避免哈希阶段越权）；持久化状态用 §2.3 snake_case（即 `import_jobs.state` 落库值）为真值，对外事件阶段用 04 kebab-case，二者经 `toDtoStage` 桥接。
  > 自验证：等价 Node 端口镜像三纯模块逻辑，`.ralph/statemachine-test/test.mjs` **102/102 PASS**（13 条 happy 边 + 9 条非法边拒绝 + 可取消/终态/阶段码/重试分类/DTO 映射 + 暂存路径派生与穿越拒绝 + StagingArea 内存 FS 编排 + SHA-256 全匹配/不符/缺失/算法错/未列/自身排除/大小写不敏感 + toError 脱敏不含哈希 + computeStagingHashes 往返）；三纯模块经 `tsc --strict --noEmit` 通过（唯一报错为既有 `common.ets` JsonObject TS2456，P0-2 已记 ArkTS↔tsc 已知分歧）。未编译验证：缺 HarmonyOS SDK——`ImportJobRepository`（relationalStore）、`ImportFs`/`HashComputer` 生产实现（`@ohos.file.fs`/`@kit.CryptoArchitectureKit`）仅引用自洽核查，待 P1-4/P1-5 接线与 SDK 实测。
- [x] P1-4 导入事务与索引落库：协议→DB 映射（《06》全文）、章节/小节/内容块/练习/空位/答案/复习卡/复核标记落库、chapter_count 事务内重算；原子提升 + 提交（§2.4 step11-13）。
  > 落地四件：①`services/import/AnswerNormalizer.ets`——§4.3 七步标准化纯函数（导入端与判分端共用，《06》§4.2 第 1 条），开关来源 `gradingOptions`、默认值集中于 `DEFAULT_NORMALIZE_OPTIONS`（不硬编码）。②`services/import/ContentMapper.ets`——协议 JSON→全表行模型纯映射器：plain_text 按 11 种 block.type 派生（《06》§3.2）+ 块来源页继承小节（§3.1）；小节 confidence←sourceRef（§2.1）；`accepted_answers` 标准化+去重合并（R-035）+主答案选取（R-030，每空恰一 is_primary）；`grading_rules`←partialRules（§4.4 R-003）+反馈码受控词归一（§5，非词表 id→other 并存 params.sourceId）；`review_cards`←flashcards（§3.19.1/§7，source_id=`<bookId>:<flashcardId>`、新卡默认）；`content_review_flags`←report.reviewQueue（§8）+IMP-015 警告判定（§2.5 R-037：reviewQueue 非空/任一章节 needs-review/report completed-with-warnings）；`assets`←包内 assets/ 图片（sha256/size 注入，不解码图片故尺寸留空）+ illustration 块 asset_id 最佳努力链接；阻断校验 IMP-020（未知题型）/IMP-012（手写题缺答案或无 blank 段）/IMP-011（toc↔章节 id 不一致、章节 id/order 重复）/IMP-010（目录章节文件缺失、封面缺失）；非手写四种题型导入保留 enabled=1、不产生空位（R-063）。内部主键经 `IdFactory` 注入（纯函数）。③`repositories/BookContentRepository.ets`——`indexBook(model, onIndexed?)`：单事务内按外键安全顺序（books→assets→chapters→sections→content_blocks→exercises→exercise_blanks→accepted_answers→grading_rules→review_cards→content_review_flags）落库 11 表 + 事务内 `SELECT COUNT(*)` 重算 chapter_count 写回（§3.3 R-047）+ 提交前钩子供原子提升；review_cards 用 `ON CONFLICT(source_type,source_id) DO UPDATE` 保留调度字段仅更新内容（§3.19.1）。④`services/import/ImportIndexer.ets`——step11~13 编排：沿 §2.3 合法路径 awaiting_confirmation→indexing→promoting_to_final→committing→completed 守卫式推进，落库+暂存→正式目录原子提升（fs.move）+提交作为一个事务原子完成（失败整体回滚、清理正式目录、置 failed）；错误映射 R-056（内容类按原码、落库兜底 IMP-019、提升/提交失败 IMP-016）。⑤`utils/UuidIdFactory.ets`——`IdFactory` 生产实现（`util.generateRandomUUID`，保持 ContentMapper 纯净）。
  > 设计裁决（非规格冲突）：`exercise.sourceRef.answerSource` 协议可选 → 缺省 `ai-inferred`（CHECK 必为四值之一，AI 生成包默认）；空位 `widthEm` 缺省 8em（落 §3.9 CHECK 3..40）；priority 取数组下标+1（《06》§4.4「数组下标（从 1）」）。assets 表已落库但**图片像素尺寸（width/height）本版留空**（不解码图片，非删字段——尺寸属 F05 渲染期可补，FK 安全）。relationalStore 单事务不可嵌套，故 indexing/promoting/committing 三个状态标记在原子落库前后包裹（崩溃恢复语义归 P1-5 §2.6）。
  > 自验证：`.ralph/index-test/test.mjs` 等价 Node 端口 **104/104 PASS**（§4.3 标准化 10 例含 NFC/弯引号/末尾标点/caseSensitive；book/章节字段映射；§3.1/§3.2 派生与 8 种 block.type plain_text；§4.2 去重 3→2 行+主答案+display 不在 accepted 插入；§4.4/§5 partialRules+受控词归一；§3.19.1 复习卡；§3.7 assets+asset_id 链接；IMP-015 三触发源；R-063 非手写保留；IMP-010/011/012/020 阻断各分支）；`ContentMapper`/`AnswerNormalizer` 纯链经 `tsc --strict --noEmit` 通过（唯二报错为既有 common.ets/JsonSchemaValidator.ets 的 JsonValue TS2456，P0-2/P1-2 已记 ArkTS↔tsc 分歧）。未编译验证：缺 HarmonyOS SDK——`BookContentRepository`（relationalStore）、`ImportIndexer`（fs.move 经 ImportFs 端口）、`UuidIdFactory`（util）生产实现仅引用自洽核查，待 P1-5 接线与 SDK 实测。
- [x] P1-5 取消/重试/后台/重启恢复 + 升级子流程：LLD §2.6/§2.7（cancel 边界、可重试分类、awaiting_confirmation 24h、升级 snapshot/promote/restore）。
  > 落地五件：①`services/import/UpgradePlan.ets`——纯规划器：把 `ContentMapper` 对新包产出的 `BookIndexModel` 按 (chapter/section/exercise) 的 `external_id` 锚定到旧书内部 id（《06》§10 R-006），产出「匹配→UPDATE（沿用旧 PK）/新增→INSERT/被删→removed*」执行计划并翻译全部子行父引用（section.chapterId/block.sectionId/exercise.chapterId/blank.exerciseId/rule.exerciseId/reviewCard.chapterId），从而 attempts/wrong_items/review_cards 冗余·外键列自然一致、历史不丢（FR-F03-06、验收 5）。②`repositories/BookContentRepository.ets` 增 `findBookByPackageId`/`setBookStatus`/`readExistingIndex`/`upgradeBook`——单事务按 FK+UNIQUE(order_no) 安全顺序原地落库（books UPDATE 保留 imported_at；assets/flags 整体替换；删被删小节级联块；chapters/sections/exercises「vacate→insert→update」两阶段重排规避 UNIQUE 冲突；被删练习 enabled=0 保留 attempts；子行清空替换；chapter_count 重算）+ 提交前原子提升钩子。③`services/import/UpgradeIndexer.ets`——§2.7 编排：单飞守卫（§8）→ status=updating → snapshot_old（旧目录改名回滚点）→ 原地落库+原子提升（同事务）→ completed；任一步失败 restore_old（删新目录·恢复回滚点·回滚事务·status 回 ready·任务 failed），绝不残留半本书。④`services/import/ImportLifecycle.ets`——取消（R-014：写库前可取消、清暂存+释放 URI 授权+置 cancelled 不写 error_code/不计 warning）、重试（R-015：仅 failed 且可重试码 IMP-013/016/017/019 允许 failed→copying 复用 jobId 重建暂存；内容类终态拒绝）、放弃（释放授权+清理）+ `UriPermissionPort` 授权释放端口。⑤`services/import/ImportRecovery.ets`——重启恢复（R-017）：扫描非终态 import_jobs，纯 `classify`（awaiting_confirmation 超 24h→自动取消、未超→挂起；selected 中断→取消；indexing 前中断→可重试失败 IMP-017；indexing/promoting/committing 中断→回滚 failed IMP-019 清半成品）+ 执行器。`ImportJobRepository` 增 `countActiveJobsForPackage`（单飞）。
  > 设计裁决：①升级删章节按《06》§10 保留章节行（attempts FK 历史）+ 被删练习 enabled=0，与 §3.3「chapter_count=COUNT(chapters)」存在展示口径张力——已记 ADR「实现期张力：升级删章节与 chapter_count 口径（P1-5）」，按优先级链取「保留历史」一侧、展示口径开放给 F04/业主（解决而非降级，未私改规格）。②重启恢复对写库阶段中断仅清暂存+置 failed；正式目录孤儿（崩溃窗口极窄、无对应 DB 书行→非「半本书」）留待派生目录 GC（import_jobs 不持 bookId）。③错误码均在既有 IMP 表内确定性分派（中断 pre-index→IMP-017、写库→IMP-019、升级提升/提交→IMP-016），不新增码。④awaiting 24h 截止以 started_at 为锚点（验证阶段耗时通常数秒、对 24h 窗口可忽略的保守近似）。
  > 自验证：`.ralph/lifecycle-test/test.mjs` 等价 Node 端口 **67/67 PASS**（状态机取消可达性/重试回边；lifecycle cancel/retry 守卫全分支；recovery.classify 各状态+24h 边界；planUpgrade 匹配·新增·被删·父引用翻译+空 existing 纯新增）；`.ralph/lifecycle-test/upgrade_sql.py` 把 `upgradeBook` SQL 序列等价端口到 **真 SQLite（V001 建表）执行 14/14 PASS**（ATT1/W1 历史 FK 保持、X1 匹配 enabled=1+prompt 更新、X2 被删 enabled=0、C1 重排 order=2 与 C3 新增 order=1 经 vacate 无 UNIQUE 冲突、books 原地更新保留 imported_at、chapter_count=3 重算、子行替换+答案级联、`PRAGMA foreign_key_check` 无违例）；纯链 `UpgradePlan` 经 `tsc --strict --noEmit` 通过（唯二既有报错为 common/JsonSchemaValidator 的 JsonValue TS2456，P0-2/P1-2 已记 ArkTS↔tsc 分歧）。未编译验证：缺 HarmonyOS SDK——`BookContentRepository`/`ImportJobRepository`（relationalStore）、`UpgradeIndexer`/`ImportLifecycle`/`ImportRecovery`（`@ohos.file.fs` 经 `ImportFs` 端口、URI 授权端口）生产实现仅引用自洽核查，待 F02/F03 接线与 SDK 实测。

## 阶段 2 — 各功能垂直切片（Repository → Service → ViewModel → 页面 → 自验）
- [x] F01 书架页：BooksRepository.list + 书架页 + 重启恢复 continueTarget（FR-F01，LLD §6.1）。
  > 落地：`repositories/BooksRepository.ets`（list/get/markBroken 三法；completionPercent 仅 lesson/review 章节平均、continueTarget=最近打开未完成章节、coverUri 由 books+reading_progress 聚合派生；默认排除 status=deleted §1.3/§8.1）+ `viewmodels/BookshelfVM.ets`（loading/content/empty/error + 排序/关键字）+ `pages/bookshelf/BookshelfPage.ets` 与 `components/bookshelf/*`（AppRail/BookGrid/BookCard/ImportButton/ImportToast，空/正常/导入中/损坏四态，卡片≥230vp、封面≈3:4、LazyForEach 懒加载）+ `pages/Index.ets` 改 @Provide('pageStack')。listChapters/searchChapters 留 F04。自验证 `.ralph/f01-test` 真 SQLite 19/19 PASS。未编译验证：缺 SDK，ArkUI 仅引用自洽。
- [x] F02 导入选择页：ImportService.create + 选择/空间检查页（FR-F02，LLD §6.2）。
  > 落地：`services/ImportService.ets`（create 注册 import_jobs=selected §2.3 + §8.1 单飞、cancel 委托 ImportLifecycle R-014；纯编排经仓储，SDK IO 抽端口；runValidation/commit 留 F03）+ `services/StorageProbe.ets`（空间充足性纯判定）+ `viewmodels/ImportSelectVM.ets`（未选择/已选择/空间不足/类型错误/复制中 五态 + 防重复点击）+ `pages/import/ImportSelectPage.ets` 与 `components/import/*`（ImportStepper/FilePickerCard/SelectedFileRow，仅单文件、不展示绝对路径）。自验证 `.ralph/f02-test` 真 SQLite+纯逻辑 35/35 PASS。未编译验证：缺 SDK，DocumentViewPicker/@ohos.file/SHA-256/URI 授权端口仅引用自洽。
- [ ] F03 校验结果页：runValidation 进度流 + 可读失败原因 + 回滚（FR-F03，LLD §6.3）。
- [ ] F04 目录页：listChapters + searchChapters(章节级) + 完成度展示（FR-F04，LLD §6.4）。
- [x] F05 章节讲解页：getChapter + 11 种 block 渲染组件（含 BulletList/AudioTextBlock，§6.5 映射表）+ 默认小节/空 sections（R-034）。
  > 落地：`repositories/ContentRepository.ets`（getChapter 组装 ChapterContent + payload_json/source_pages_json 解析与损坏降级 + illustration LEFT JOIN assets 带 assetUri + getAssetUri/getCoverUri）+ `viewmodels/LessonVM.ets`（loading/ready/empty/broken + R-034 默认小节 ?section>last_section_id>下标0 + 锚点 AnchorPort 端口）+ `pages/lesson/LessonPage.ets` 与 `components/lesson/*`（LessonHeader/LessonTabs/ContentBlockRenderer 分发/ChapterDrawer 抽屉换章禁左右滑动/ChapterEmptyView + 11 种 block 渲染 + UnsupportedBlock R-036 兜底）。自验证 `.ralph/f05-test` 真 SQLite 53/53 PASS。未编译验证：缺 SDK，ArkUI 渲染组件仅引用自洽。listChapters 为 F05 临时本地实现，正式契约待 F04 切换。
- [ ] F06 手写练习页：笔迹采集/即时墨迹/防误触、blank 多空、draft 快照（§4.1/§6.6），单位换算 emToVp（§1.3）。
- [ ] F07 识别与判分页：HandwritingRecognizer 封装 + RecognitionHandle/取消 + GradingEngine 纯函数 + GradingService 编排 + 逐空 perBlank（§4.2/§4.4/§4.5）；识别失败/降级（R-065）。
- [ ] F08 批注页：AnnotationRepository/Service（笔记/高亮/收藏 + 自动保存 + 退出落盘 + 重定位），handwriting_records 三态（§3.15/§6.8）。
- [ ] F09 复习页：ReviewScheduler（间隔算法 §5 + applyRating + getTodaySummary + 会话去重）+ 复习页（FR-F09，LLD §6.9）。
- [ ] F10 错题本页：WrongBookRepository(query/getDetail/listForBatch/markMastered 不覆盖历史) + 筛选/重做/批量页（FR-F10，§6.10）。
- [ ] F11 学习统计页：StatisticsService(recordEvent/getMetrics/recompute) + 统计口径(§3.13.1/§3.13A) + 图表+文本摘要（FR-F11，§6.11）。
- [ ] F12 设置与数据页：SettingsService 键级校验 + 导出/恢复(BackupService 兼容判定) + 清缓存白名单 + 两阶段删除(§8.1)（FR-F12，§6.12）。

## 阶段 3 — 非功能、安全与收尾
- [ ] P3-1 regex 判分 ReDoS 防护：导入期静态校验 + 运行期输入上限/50ms 硬超时降级（LLD §3.11、HLD §11）。
- [ ] P3-2 缓存策略与文件版本：LRU/容量(§9A)、笔迹文件 version 兼容、备份 formatVersion 兼容判定。
- [ ] P3-3 NFR 落实核对：性能/可靠性/安全/无障碍/兼容性（SRS §8 NFR-P/R/S/A/C）逐条对照实现。
- [ ] P3-4 测试用例：实现 LLD §11 的 TC-U-001~008 单元测试 + TC-I-001~007 集成测试（能在环境里跑的优先）。
- [ ] P3-5 功能完成矩阵：新建/更新「功能完成矩阵」文档，对照 SRS §9 与 §10 总体验收逐项打勾，列出未编译验证项。

## 日志
（每轮在此追加：`- [x] <条目> @<commit短哈希> — <一句话>`）
- [x] P0-1 HarmonyOS 工程脚手架 @41bea58 — 补齐 AppScope/app.json5、entry module.json5/build-profile/oh-package/hvigorfile/obfuscation、EntryAbility 与 pages/Index 入口、base 资源（string/color/main_pages）及 LLD §1.2 ets 目录骨架（.gitkeep 占位）。
- [x] P0-2 通用类型与 DTO @556db45 — `domain/entities/` 11 个 ArkTS 模型对齐《04》§1~§11（本轮恢复提交：上一轮 PowerShell 工具异常落盘未提交）。
- [x] P0-3 数据库迁移 V001 @531702a — V001__initial.sql（22 业务表 + 17 索引，§3.3~§3.25）+ SqlScript/MigrationRunner/Database/MigrationRegistry；DDL 经 SQLite 解析建表 + 约束冒烟测试全通过，relationalStore 封装与迁移执行器（迁移表/单调递增/单事务/只执行一次/可回滚）就绪。
- [x] P0-2 通用类型与 DTO @556db45 — 在 `domain/entities/` 新增 11 个 ArkTS 模型文件对齐《04》§1~§11（DTO + 19 接口契约），经 `tsc --strict --noEmit` 交叉核验通过。（提交在 ralph 本轮恢复：上一轮 PowerShell 工具异常 Exit 66 落盘未提交，本轮补提交为 @556db45。）
- [x] P0-4 Repository 基类与事务封装 @1d9c2fb — 新增 `utils/AppErrors.ets`（§3.14A 码→资源键映射 25 条逐行一致 + 领域错误工厂/脱敏）、`repositories/BaseRepository.ets`（页面不碰 SQL、ResultSet 安全遍历、查询异常归一），并给 `database/Database.ets` 增 `withTransaction` 事务/可回滚封装；映射经 Python 与规格逐行比对 PASS。
- [x] P0-5 i18n 资源骨架 @54ac27f — 三份 `resources/{base,zh_CN,en_US}/element/string.json` 落 §3.14A 全 25 键 + §3.14B 4 通用键中英文文案；登记并裁决 i18n 点分键 ↔ HarmonyOS `restool` 命名实现期冲突（保留逻辑键真值、物理名 `.`→`_` 转写、`AppErrors.physicalKeyFor` 桥接）；JSON 解析/全键齐备/命名合法/给定文案逐字一致核验 PASS。
- [x] P0-6 路由骨架 @afd54c9 — `utils/Routes.ets`（10 路由名 + 8 参数接口 + §6.13 三归一化纯函数 + ROUTE_TABLE）、`resources/base/profile/route_map.json` 系统路由表（9 NavDestination）+ module.json5 `routerMap`、`Index.ets` 改 Navigation 容器根页、9 占位页 + `BookshelfPage` + `RoutePlaceholder` 复用组件、三语补 11 条路由标题/占位文案；交叉一致性脚本 + `tsc --strict` + 归一化 13 例单测全 PASS（缺 SDK 未编译）。
- [x] P1-1 ZIP 安全校验 @ba9bc96 — `services/import/` 新增 `ImportLimits.ets`（§2.1 阈值/允许扩展名/`defaultValidationOptions`）、`ZipCentralDirectory.ets`（不解压的纯函数中央目录枚举器，自带 UTF-8 解码、ZIP64 哨兵安全拒绝）、`ZipSafetyValidator.ets`（逐条目判定 → IMP-001~004，路径穿越/资源限制/压缩炸弹）；Python 制 11 真实夹具经等价 Node 端口 13/13 PASS（对齐 TC-U-001/002）。错误码在既有 IMP-001~004 内确定性分派（伪造扩展名/路径长度→IMP-003，非新增码）。未编译验证：缺 SDK，文件字节读取留待 P1-3 接线。
- [x] P1-2 内置 JSON Schema 校验器 @41bd53f — `services/import/` 新增 `JsonSchemaValidator.ets`（draft 2020-12 子集纯函数引擎，违规只记结构化原因不含实例值）、`StudybookSchemaCatalog.ets`（文件→种类分类器，忽略包内 `schemas/`/`assets/` 封死信任边界 BR-002 + `SchemaProvider`/`InMemorySchemaProvider`）、`PackageSchemaValidator.ets`（解析 IMP-007 + 校验 IMP-008 编排），并把五个 Schema 复制进 `resources/rawfile/schemas/` 作内置可信副本；等价 Node 端口 40/40 PASS（逐关键字失败用例 + 非手写占位不强约束 R-063 + 关键字覆盖元测试）。`format` 按 2020-12 默认作注解不断言。未编译验证：缺 SDK，rawfile Schema 读取留待 P1-3 接线。
- [x] P1-3 导入状态机与暂存区 @4558598 — `services/import/` 新增 `ImportStateMachine.ets`（§2.3 逐边 15 态转移表 + 守卫/取消/终态判定 + 阶段→IMP-* 码 R-056 + 可重试分类 R-015 + 状态→04 事件阶段映射）、`ImportStaging.ets`（§2.2 沙箱路径派生 + 路径段/相对路径安全 + `ImportFs` 端口 + `StagingArea` prepare/cleanup/枚举/读取）、`ChecksumVerifier.ets`（§2.4 step7 纯比对→IMP-009 脱敏不含哈希 + `HashComputer` 端口），`repositories/ImportJobRepository.ets`（§3.22/§3.23 状态机持久化，事务内守卫式 `transition`）；`.ralph/statemachine-test` 102/102 PASS + 三纯模块 `tsc --strict` 通过（仅既有 common.ets TS2456）。未编译验证：缺 SDK，relationalStore/`@ohos.file.fs`/crypto 生产实现留待 P1-4/P1-5 接线。
- [x] P1-4 导入事务与索引落库 @5232e5f — `services/import/` 新增 `AnswerNormalizer.ets`（§4.3 标准化纯函数，导入/判分共用）、`ContentMapper.ets`（协议→全表行模型纯映射：plain_text 派生§3.2/来源页继承§3.1/答案去重+主答案§4.2/规则映射§4.4+受控词§5/复习卡§3.19.1/复核标记+IMP-015§8/assets+asset_id 链接；阻断校验 IMP-010/011/012/020；非手写题保留 R-063）、`ImportIndexer.ets`（step11~13 编排：守卫式状态推进 + 单事务落库+原子提升+提交+失败回滚清理）；`repositories/BookContentRepository.ets`（单事务 FK 安全顺序落 11 表 + 事务内 chapter_count 重算 R-047 + 提交前提升钩子 + review_cards ON CONFLICT upsert）；`utils/UuidIdFactory.ets`（IdFactory 生产实现）。`.ralph/index-test` 104/104 PASS + 纯链 `tsc --strict` 通过（唯二既有 JsonValue TS2456）。未编译验证：缺 SDK，relationalStore/fs.move/util 生产实现待 P1-5 接线。
- [x] P1-5 取消/重试/后台/重启恢复 + 升级子流程 @7a28fa1 — `services/import/` 新增 `UpgradePlan.ets`（按 external_id 锚定旧书内部 id 的纯规划器，匹配 UPDATE/新增 INSERT/被删 removed + 子行父引用翻译，《06》§10）、`UpgradeIndexer.ets`（§2.7 单飞守卫→updating→snapshot_old→原地落库+原子提升→completed，失败 restore_old 绝不残留半本书）、`ImportLifecycle.ets`（取消 R-014/重试 R-015 failed→copying 复用 jobId/放弃 + URI 授权释放端口）、`ImportRecovery.ets`（重启恢复 R-017 扫描非终态任务，纯 classify：awaiting 24h 自动取消/indexing 前可重试失败 IMP-017/写库阶段回滚 failed IMP-019 清半成品）；`repositories/BookContentRepository.ets` 增 `findBookByPackageId/setBookStatus/readExistingIndex/upgradeBook`（vacate→insert→update 两阶段重排 + 被删练习 enabled=0 保留 attempts + chapter_count 重算）、`ImportJobRepository.ets` 增 `countActiveJobsForPackage` 单飞计数。Node 端口 67/67 PASS + `upgradeBook` 真 SQLite 执行 14/14 PASS（历史 FK 保持/重排无 UNIQUE 冲突/foreign_key_check 无违例）+ 纯链 `tsc --strict` 通过（唯二既有 TS2456）。升级删章节与 §3.3 chapter_count 口径张力已记 ADR。未编译验证：缺 SDK，relationalStore/@ohos.file.fs/URI 授权端口生产实现待 F02/F03 接线。
- [x] F01 书架页 @1e9cf1c — `repositories/BooksRepository.ets`(list/get/markBroken；completionPercent 仅 lesson/review 平均、continueTarget 最近未完成章节、coverUri 聚合派生、默认排除 deleted) + `viewmodels/BookshelfVM.ets`(四态+排序/关键字) + `pages/bookshelf/BookshelfPage.ets`+`components/bookshelf/*`(AppRail/BookGrid/BookCard/ImportButton/ImportToast、空/正常/导入中/损坏、LazyForEach) + `pages/Index.ets` 改 @Provide('pageStack')。并行批次主 agent 整合三份 string.json 共 44 键（F01/F02/F05）。`.ralph/f01-test` 真 SQLite 19/19 PASS。未编译验证：缺 SDK。
- [x] F02 导入选择页 @64381e0 — `services/ImportService.ets`(create 注册 selected+单飞、cancel 委托 ImportLifecycle；runValidation/commit 留 F03) + `services/StorageProbe.ets`(空间判定端口) + `viewmodels/ImportSelectVM.ets`(五态+防重复点击) + `pages/import/ImportSelectPage.ets`+`components/import/*`(ImportStepper/FilePickerCard/SelectedFileRow、仅单文件、不展示绝对路径)。`.ralph/f02-test` 真 SQLite+纯逻辑 35/35 PASS。未编译验证：缺 SDK（picker/file/crypto/URI 端口）。
- [x] F05 章节讲解页 @7016ae1 — `repositories/ContentRepository.ets`(getChapter 组装+payload/sourcePages 解析与损坏降级+illustration assetUri 联表+getAssetUri/getCoverUri) + `viewmodels/LessonVM.ets`(四态+R-034 默认小节+锚点端口) + `pages/lesson/LessonPage.ets`+`components/lesson/*`(LessonHeader/LessonTabs/ContentBlockRenderer/ChapterDrawer 禁左右滑动/ChapterEmptyView + 11 种 block 渲染 + UnsupportedBlock R-036)。`.ralph/f05-test` 真 SQLite 53/53 PASS。未编译验证：缺 SDK。listChapters 临时本地实现待 F04 切换。
